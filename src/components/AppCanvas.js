import styled from "styled-components";

const AppCanvas = styled.main`
  width: 400px;
  height: 600px;
  margin: 0 auto;
  border-radius: 8px;
  box-shadow: 0 0 18px rgba(0, 0, 0, 0.08);
  background: white;
`;

export default AppCanvas;
